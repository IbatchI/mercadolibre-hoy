import styled from 'styled-components'
import { mainTheme } from '../../../../styles/Styles'

export const Error = styled.p`
  color: ${mainTheme.colors.dangerColor};
  font-size: 0.9rem;
  margin: 0;
  padding: 0;
  width: 100%;
  padding-left: 5px;
  font-weight: ${mainTheme.fontWeights.normal};
`
